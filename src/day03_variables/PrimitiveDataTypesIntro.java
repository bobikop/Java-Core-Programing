package day03_variables;

  public class PrimitiveDataTypesIntro {

       // DataType variableName = Data   ---- this is a syntax for declaring data types in Java
       // byte a = "Java"; // byte only accepts integer numbers (-128 to 127)
      // byte a = 2.5; // byte can not take decimal number
      // byte a = 2000; // out of Byte's range (-128 to 127)
    byte a = 20;

      // price of the car is $17500
    short p = 17500;

      // salary is 30000
    int s = 9500;
      // int number = 999999999999; out of  range from ing, so we need to use long
      long number = 9999999999l;
      double gpa1 = 3.5; //prefered
      float gpa2 = 3.5f;

}
